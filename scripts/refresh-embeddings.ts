/**
 * Re-embed menu items/translations/modifiers when they change (design §7).
 * Stub. TODO: embed with EMBEDDING_MODEL and upsert into menu_embeddings.
 */
console.log('refresh-embeddings: stub — embed menu vectors and upsert into menu_embeddings');
